import React from 'react';
import { Text } from 'react-native';

interface LiveCardProps {}

const LiveCard: React.FC<LiveCardProps> = props => {
  return <Text>TODO</Text>;
};

export default LiveCard;
